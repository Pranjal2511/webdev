void main() {
  List<String> names = ["ramesh", "Mukesh", "Dhruv"];
  //print length

  // var count = 0;
  // for (int i = 0; i < names.length; i++) {
  //   print(names[i]);
  //   print(names.length);
  // }

  // using membership operator

  for (var a in names) {
    print(a);
  }
}
